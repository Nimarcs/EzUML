// PACKAGE
package modele.classe;

import java.io.Serializable;

/**
 * Enumeration TypeClasse, permet de definir le type de classe qu'on cree
 */
public enum TypeClasse implements Serializable {
    CLASSE,
    INTERFACE,
    ENUM,
    ABSTRACT
}
